<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="technovation.tuty_boxapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:icon="@mipmap/logo_bordo"
        android:label="Tuty-Box"
        android:roundIcon="@mipmap/logo_bordo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize|stateHidden">
        <activity
            android:name=".LoginActivity"
            android:label="Tuty-Box">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".GeneralInformation"/>
        <activity android:name=".ChooseJob"/>
        <activity android:name=".AddTeacherDetails"/>
        <activity android:name=".AddStudentDetails"/>
        <activity android:name=".AddBothStudentDetails"/>
        <activity android:name=".FinishingSignUp"/>
        <activity android:name=".WriteDemands"/>
        <activity android:name=".WriteClass"/>
        <activity android:name=".CurrentClasses">
        </activity>
    </application>

</manifest>